<!--var id = '<%#= @review.id %>'-->
<!--var reviewer_nurse_id = '<%#= @review.reviewer_nurse_id %>'-->
<!--var schedule_id = '<%#= @review.schedule_id %>'-->
<!--var review= '<%#= @review.review %>'-->
var score = '<%= @review.score %>'
var url = "/schedules/'<%= @review.schedule_id %>'/reviews";
var data = { reviewer_nurse_id: '<%= @review.reviewer_nurse_id %>', schedule_id: '<%= @review.schedule_id %>', review: '<%= @review.review %>', score: '<%= @review.score %>' };

if(score < 0 ){
  if( confirm('感情分析結果は' + score + '点でした。マイナスな表現が含まれますが、投稿しますか？') ){
    location.href = "/";
  }
}else{
  if( confirm('感情分析結果は' + score + '点でした。投稿しますか？') ){
    $.ajax({
    type: 'POST',
    url: url,
    data: data,
    dataType: 'text'
    })
  }
}